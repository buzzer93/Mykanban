import './stimulus_bootstrap.js';
/*
 * Point d'entrée JS — chargé via l'importmap dans base.html.twig.
 *
 * Le CSS (Tailwind) n'est PAS importé ici : il est émis par {{ tailwind_stylesheet() }}
 * dans le layout Twig. Le build/watch est géré par `php bin/console tailwind:build`.
 */
